{
  "name": "jedi-party-scheduler-worker",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env \"\"",
    "deploy:sandbox": "wrangler deploy --env sandbox",
    "typecheck": "tsc --noEmit && tsc --noEmit -p test/tsconfig.json",
    "db:migrate:local": "wrangler d1 migrations apply jedi-party-scheduler-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply jedi-party-scheduler-db --remote",
    "db:migrate:status": "wrangler d1 migrations list jedi-party-scheduler-db --remote",
    "db:migrate:local:sandbox": "wrangler d1 migrations apply jedi-party-scheduler-db-sandbox --local --env sandbox",
    "db:migrate:remote:sandbox": "wrangler d1 migrations apply jedi-party-scheduler-db-sandbox --remote --env sandbox",
    "db:migrate:status:sandbox": "wrangler d1 migrations list jedi-party-scheduler-db-sandbox --remote --env sandbox",
    "db:verify": "node scripts/verify-schema.mjs",
    "check:env-parity": "node scripts/check-env-parity.mjs",
    "seed:sandbox": "wrangler d1 execute jedi-party-scheduler-db-sandbox --remote --env sandbox --file scripts/seed-sandbox.sql",
    "seed:poll-demo": "wrangler d1 execute jedi-party-scheduler-db-sandbox --remote --env sandbox --file scripts/seed-poll-demo.sql",
    "seed:button-demo": "wrangler d1 execute jedi-party-scheduler-db-sandbox --remote --env sandbox --file scripts/seed-button-demo.sql",
    "seed:resolve-demo": "wrangler d1 execute jedi-party-scheduler-db-sandbox --remote --env sandbox --file scripts/seed-resolve-demo.sql",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.6.3",
    "luxon": "^3.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20250101.0",
    "@types/luxon": "^3.4.2",
    "@types/node": "^26.1.2",
    "typescript": "^5.6.2",
    "vitest": "^3.2.7",
    "wrangler": "^4.20.0"
  }
}
